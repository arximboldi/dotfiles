#!/usr/bin/env bash

source ~/.bash.d/init.bash

if pidof emacs || pidof .emacs-wrapped; then
    notify-send -t 3000 -i emacs "emacs" "already running!"
else
    notify-send -t 3000 -i emacs "emacs" "starting..."
    emacs --daemon
fi
