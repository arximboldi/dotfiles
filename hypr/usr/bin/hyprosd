#!/usr/bin/env bash

# sometimes it crashes when changing monitors
if ! pidof swayosd-server > /dev/null; then
    swayosd-server &
fi

# repeat three times to ensure the server is ready
for i in {1..10}; do
    if swayosd-client "$*"; then
        exit 0
    fi
    sleep 0.1
done
