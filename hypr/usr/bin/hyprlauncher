#!/usr/bin/env bash

if [ -d "$HOME/usr/bin" ] ; then
    export PATH="$HOME/usr/bin:$PATH"
fi

# dmenu-wl_path \
#    | grep -v "~$" \
#    | dmenu-wl -i -b -fn "Cantarell Bold 18" -h 48 -nb "#1a1b2688" -nf "#dddddd" -sb "#BD93F9" -sf "#1a1b26" \
#    | ${SHELL:-"/bin/sh"} &

dmenu-wl_path \
    | grep -v "~$" \
    | wofi --show dmenu --insensitive --prompt "" \
    | ${SHELL:-"/bin/sh"}

# wofi --show run --insensitive
